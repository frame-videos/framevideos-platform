---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { SignupForm } from '@/components/react/SignupForm';
---

<BaseLayout title="Criar Conta — Frame Videos">
  <div class="flex min-h-screen items-center justify-center bg-dark-950 px-4 py-12">
    <!-- Background -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute top-1/3 left-1/2 -translate-x-1/2 w-[600px] h-[400px] rounded-full bg-primary-600/10 blur-[120px]"></div>
    </div>

    <div class="w-full max-w-md">
      <!-- Logo -->
      <a href="/" class="flex items-center justify-center gap-2 mb-8">
        <div class="flex h-10 w-10 items-center justify-center rounded-lg bg-primary-600">
          <svg class="h-6 w-6 text-white" viewBox="0 0 24 24" fill="currentColor">
            <polygon points="8,4 8,20 20,12" />
          </svg>
        </div>
        <span class="text-xl font-bold text-white">Frame Videos</span>
      </a>

      <!-- Form card -->
      <div class="rounded-xl border border-border bg-surface p-8">
        <h1 class="text-2xl font-bold text-white mb-2">Criar Conta</h1>
        <p class="text-sm text-dark-400 mb-6">
          Comece grátis. Sem cartão de crédito.
        </p>

        <SignupForm client:load />
      </div>

      <p class="mt-6 text-center text-sm text-dark-400">
        Já tem uma conta?{' '}
        <a href="/login" class="text-primary-400 hover:text-primary-300 font-medium transition-colors">
          Fazer login
        </a>
      </p>
    </div>
  </div>
</BaseLayout>
